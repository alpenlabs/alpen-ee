# Setup for Ethereum Execution Spec Tests (EEST).
# Runs alpen-client in sequencer mode with dummy OL + regtest bitcoind for DA.
#
# Usage:
#   ./init-alpen-client-keys.sh
#   docker compose --env-file .env.alpen-client -f docker-compose-eest.yml up

services:

  bitcoind:
    build:
      context: ./bitcoind/
    image: strata_bitcoind:v0.1.2
    container_name: eest_bitcoind
    environment:
      RPC_ALLOW_IP: "0.0.0.0/0"
      BITCOIND_RPC_USER: ${BITCOIND_RPC_USER:-rpcuser}
      BITCOIND_RPC_PASSWORD: ${BITCOIND_RPC_PASSWORD:-rpcpassword}
      BITCOIND_WALLET: default
      GENERATE_BLOCKS: 101
    volumes:
      - ./bitcoind/entrypoint.sh:/app/entrypoint.sh
    ports:
      - "18443:18443"
    healthcheck:
      test: ["CMD-SHELL", "bitcoin-cli -regtest -rpcuser=${BITCOIND_RPC_USER} -rpcpassword=${BITCOIND_RPC_PASSWORD} getwalletinfo"]
      interval: 2s
      timeout: 5s
      retries: 30
      start_period: 5s
    networks:
      - eest

  alpen-client:
    image: alpen-client:latest
    container_name: alpen_eest
    depends_on:
      bitcoind:
        condition: service_healthy
    command:
      - --sequencer
      - --sequencer-pubkey
      - ${SEQUENCER_PUBKEY}
      - --dummy-ol-client
      - --datadir
      - /app/data
      - --p2p-secret-key
      - /app/keys/seq-p2p.hex
      - --prover-backend
      - native
      - --native-chunk-signing-key
      - /app/keys/native-chunk-signing-key.hex
      - --native-acct-signing-key
      - /app/keys/native-acct-signing-key.hex
      - --addr
      - "0.0.0.0"
      - --port
      - "30303"
      - --nat
      - none
      - --disable-discovery
      - --http
      - --http.addr
      - "0.0.0.0"
      - --http.port
      - "8545"
      - --http.api
      - eth,net,web3,txpool,admin,debug
      - --btc-rpc-url
      - http://bitcoind:${BITCOIND_RPC_PORT:-18443}
      - --btc-rpc-user
      - ${BITCOIND_RPC_USER:-rpcuser}
      - --btc-rpc-password
      - ${BITCOIND_RPC_PASSWORD:-rpcpassword}
      - --l1-reorg-safe-depth
      - "${L1_REORG_SAFE_DEPTH:-1}"
      - --genesis-l1-height
      - "${GENESIS_L1_HEIGHT:-101}"
      - --batch-sealing-block-count
      - "${BATCH_SEALING_BLOCK_COUNT:-5}"
      - --color
      - never
    environment:
      SEQUENCER_PRIVATE_KEY: ${SEQUENCER_PRIVATE_KEY}
      RUST_LOG: ${RUST_LOG:-info}
      ALPEN_PARAMS_PATH: ${ALPEN_PARAMS_PATH:-/app/configs/generated/alpen-params.json}
    ports:
      - "8545:8545"
    volumes:
      - ./configs/generated/alpen-params.json:${ALPEN_PARAMS_PATH:-/app/configs/generated/alpen-params.json}:ro
      - ./configs/alpen-client/seq-p2p.hex:/app/keys/seq-p2p.hex:ro
      # Native EE prover signing keys. This service runs with
      # --dummy-ol-client, whose mock `get_latest_account_update_vk` (see
      # ol/dummy.rs) hardcodes the same fixed acct predicate
      # `EeAcctProgram::test_signing_key` produces (0x02 * 32) -- signing
      # with a different acct key will fail prover-predicate validation at
      # startup. The chunk key isn't checked externally, but 0x03 * 32 is
      # used here too for consistency with the functional-tests factory.
      - ./configs/alpen-client/native-chunk-signing-key.hex:/app/keys/native-chunk-signing-key.hex:ro
      - ./configs/alpen-client/native-acct-signing-key.hex:/app/keys/native-acct-signing-key.hex:ro
    entrypoint: ["/usr/local/bin/entrypoint.sh"]
    networks:
      - eest

networks:
  eest:
    driver: bridge
